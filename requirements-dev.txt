# 開發和 CI 用的工具，版本全部鎖死（==）。
#
# 為什麼要鎖？如果寫 ruff>=0.6，CI 每次都會裝「當下最新版」。
# 哪天工具出新版改了規則，你沒改任何程式，CI 卻突然變紅，而且很難查出原因。
# 鎖死之後，版本只會透過 Dependabot 發的 PR 更新，而且要先通過 CI 才能合併。
#
# 安裝：pip install -r requirements-dev.txt -e .
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.5
